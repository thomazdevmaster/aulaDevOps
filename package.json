{
  "name": "teste",
  "version": "1.0.0",
  "description": "1. No terminal execute `docker container run -it ubuntu /bin/bash` 2. Em seguida, no terminal do container `apt update -y && apt install curl -y && curl --version`",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.18.2"
  }
}
